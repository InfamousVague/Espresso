[package]
name = "espresso-linux"
version = "0.2.9"
edition = "2021"
description = "Linux build of Espresso (part of the MattsSoftware suite)."
license = "MIT"
publish = false

[[bin]]
name = "espresso"
path = "src/main.rs"

[dependencies]
# Filled in iteratively. Initial pick:
#   zbus      = "4"   # D-Bus client for logind Inhibit
#   ksni      = "0.2" # StatusNotifierItem tray
#   clap      = { version = "4", features = ["derive"] }
#   anyhow    = "1"
